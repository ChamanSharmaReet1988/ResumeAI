<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- API 31+ uses the system SplashScreen API instead of windowBackground.
         Without these the platform falls back to the launcher icon, which is
         cropped to the splash mask and reads as a broken icon. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@mipmap/ic_splash_icon</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
